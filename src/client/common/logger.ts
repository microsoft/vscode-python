export {
    // aliases:
    // (temporarily for backward compatibility and to avoid churn)
    TraceOptions as LogOptions,
    traceDecorators,
    traceError,
    traceInfo,
    traceVerbose,
    traceWarning
} from '../logging/_trace';
