export {
    // aliases:
    traceDecorators,
    traceError,
    traceInfo,
    traceVerbose,
    traceWarning
} from '../logging/_trace';
export { TraceOptions as LogOptions } from '../logging/types';
