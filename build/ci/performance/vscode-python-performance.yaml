name: '$(Year:yyyy).$(Month).0.$(BuildID)-weekly-performance-test'

trigger: none
pr: none
schedules:
- cron: "0 0 * * sat"
  displayName: Weekly Performance Test
  branches:
    include:
    - master

variables:
- group: DS_Performance_Benchmark

stages:
- stage: Test_performance
  jobs:
  - template: perfJobs.yaml

- stage: Results
  dependsOn:
  - Test_performance
  jobs:
  - job: CheckResults
    timeoutInMinutes: 5
    pool: DSPerf
    steps:
      - powershell: |
        echo 2020
        echo $(DataScience DataViewer tests Data Frame)
